<Page
    x:Class="PWin11_Tweaker_s.PrivacyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Spacing="15">
            <TextBlock x:Uid="SettingsPrivacy" FontSize="24" FontWeight="SemiBold"/>

            <!-- Отключение телеметрии -->
            <CheckBox x:Name="DisableTelemetryToggle" x:Uid="DisableTelemetry">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisSendDiagDatatoMicrosoft" />
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение рекламного ID -->
            <CheckBox x:Name="DisableAdvertisingIdToggle" x:Uid="DisableAdvertisingId">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableAdvertisingComment"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение отслеживания местоположения -->
            <CheckBox x:Name="DisableLocationToggle" x:Uid="DisableLocation">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableLocationComment"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение Cortana -->
            <CheckBox x:Name="DisableCortanaToggle" x:Uid="DisableCortana">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableCortanaComment"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение фоновых приложений -->
            <CheckBox x:Name="DisableBackgroundAppsToggle" x:Uid="DisableBackgroundApps">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableBackgroundAppsComment"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение отправки данных в облако -->
            <CheckBox x:Name="DisableCloudContentToggle" x:Uid="DisableCloudContent">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableCloudContentComment"/>
                </ToolTipService.ToolTip>
            </CheckBox>


            <CheckBox x:Name="DisableFindMyDeviceToggle" x:Uid="DisableFindMyDevice" Margin="0,10,0,0"/>
            <CheckBox x:Name="DisableInsiderTelemetryToggle" x:Uid="DisableInsiderTelemetry" Margin="0,10,0,0"/>
            <CheckBox x:Name="DisableEdgeDiagnosticsToggle" x:Uid="DisableEdgeDiagnostics" Margin="0,10,0,0"/>
            <CheckBox x:Name="DisableSuggestedContentToggle" x:Uid="DisableSuggestedContent" Margin="0,10,0,0"/>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                <Button x:Name="ApplyButton" x:Uid="Apply" Click="ApplyButton_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="ResetButton" x:Uid="Restore" Click="ResetButton_Click"/>
            </StackPanel>

            <!-- Прогресс и статус -->
            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="5">
                <TextBlock x:Name="StatusText" x:Uid="Status"/>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0" 
                             Foreground="{ThemeResource SystemAccentColor}" Background="{ThemeResource SubtleFillColorSecondary}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>