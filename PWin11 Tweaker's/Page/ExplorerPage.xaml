<Page
    x:Class="PWin11_Tweaker_s.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Spacing="15">
            <TextBlock Text="Настройки Проводника" FontSize="24" FontWeight="SemiBold"/>

            <!-- Показ скрытых файлов -->
            <CheckBox x:Name="ShowHiddenFiles" Content="Показывать скрытые файлы и папки">
                <ToolTipService.ToolTip>
                    <ToolTip Content="Включает отображение скрытых файлов, папок и системных файлов."/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Уменьшение кнопок -->
            <CheckBox x:Name="UseSmallCaptions" Content="Уменьшить кнопки управления окном">
                <ToolTipService.ToolTip>
                    <ToolTip Content="Уменьшает размер кнопок Закрыть/Свернуть/Развернуть."/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Классическое контекстное меню -->
            <CheckBox x:Name="ClassicContextMenuToggle" Content="Использовать классическое контекстное меню">
                <ToolTipService.ToolTip>
                    <ToolTip Content="Возвращает классическое контекстное меню Windows 10."/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Установка/удаление StartAllBack -->
            <Button x:Name="InstallStartAllBackButton" Content="Установить StartAllBack" Click="InstallStartAllBackButton_Click" Style="{StaticResource AccentButtonStyle}"/>

            <!-- Кнопка применения -->
            <Button x:Name="ApplyButton" Content="Применить" Click="ApplyButton_Click" Style="{StaticResource AccentButtonStyle}" Margin="0,10,0,0"/>

            <!-- Прогресс и статус -->
            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="5">
                <TextBlock x:Name="StatusText" Text="Выполняется..."/>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0" 
                             Foreground="{ThemeResource SystemAccentColor}" Background="{ThemeResource SubtleFillColorSecondary}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>