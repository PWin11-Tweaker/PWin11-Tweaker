<Page
    x:Class="PWin11_Tweaker_s.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:l="using:WinUI3Localizer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Spacing="15">
            <TextBlock l:Uids.Uid="Text_Settings" FontSize="24" FontWeight="SemiBold"/>

            <!-- Показ скрытых файлов -->
            <CheckBox x:Name="ShowHiddenFiles" l:Uids.Uid="ShowHiddenFiles">
                <ToolTipService.ToolTip>
                    <ToolTip l:Uids.Uid="Coment_ShowHiddenFiles"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Уменьшение кнопок -->
            <CheckBox x:Name="UseSmallCaptions" l:Uids.Uid="UseSmallCaptions">
                <ToolTipService.ToolTip>
                    <ToolTip l:Uids.Uid="Coment_UseSmallCaptions"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Классическое контекстное меню -->
            <CheckBox x:Name="ClassicContextMenuToggle" l:Uids.Uid="ClassicContextMenu">
                <ToolTipService.ToolTip>
                    <ToolTip l:Uids.Uid="Coment_ClassicContextMenu"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение папки "Главное" -->
            <CheckBox x:Name="DisableHomeFolder" l:Uids.Uid="DisableHomeFolder">
                <ToolTipService.ToolTip>
                    <ToolTip l:Uids.Uid="Coment_DisableHomeFolder"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Отключение папки "Галерея" -->
            <CheckBox x:Name="DisableGalleryFolder" l:Uids.Uid="DisableGalleryFolder">
                <ToolTipService.ToolTip>
                    <ToolTip l:Uids.Uid="Coment_DisableGalleryFolder"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Установка/удаление StartAllBack -->
            <Button x:Name="InstallStartAllBackButton" l:Uids.Uid="Content_Button_StartAllBack" Click="InstallStartAllBackButton_Click" Style="{StaticResource AccentButtonStyle}"/>

            <!-- Кнопка открытия OldNewExplorer -->
            <Button x:Name="OpenOldNewExplorerButton" l:Uids.Uid="OpenOldNewExplorer" Click="OpenOldNewExplorerButton_Click" Style="{StaticResource AccentButtonStyle}" Margin="0,10,0,0"/>

            <!-- Кнопка применения -->
            <Button x:Name="ApplyButton" l:Uids.Uid="Apply" Click="ApplyButton_Click" Style="{StaticResource AccentButtonStyle}" Margin="0,10,0,0"/>

            <!-- Прогресс и статус -->
            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="5">
                <TextBlock x:Name="StatusText" Text="Выполняется..."/>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0" 
                             Foreground="{ThemeResource SystemAccentColor}" Background="{ThemeResource SubtleFillColorSecondary}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>