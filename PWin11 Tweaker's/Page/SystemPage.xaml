<Page
    x:Class="PWin11_Tweaker_s.SystemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style TargetType="Button" x:Key="TweakerButtonStyle">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="250"/>
        </Style>
        <Style TargetType="CheckBox" x:Key="TweakerCheckBoxStyle">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SectionHeaderStyle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="10">
            <!-- Заголовок -->
            <TextBlock x:Name="TitleTextBlock" x:Uid="SystemPage.Title" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Оптимизация служб -->
            <TextBlock x:Name="ServicesSectionHeader" x:Uid="SystemPage.ServicesSectionHeader" Style="{StaticResource SectionHeaderStyle}"/>
            <Button x:Name="DisableServicesButton" x:Uid="DisableServicesButton.Content" Style="{StaticResource TweakerButtonStyle}">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableServicesButton.ToolTip"/>
                </ToolTipService.ToolTip>
            </Button>

            <!-- Безопасность -->
            <TextBlock x:Name="SecuritySectionHeader" x:Uid="SystemPage.SecuritySectionHeader" Style="{StaticResource SectionHeaderStyle}"/>
            <CheckBox x:Name="DisableUACCheckBox" x:Uid="DisableUACCheckBox.Content" Style="{StaticResource TweakerCheckBoxStyle}">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableUACCheckBox.ToolTip"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Конфиденциальность -->
            <TextBlock x:Name="PrivacySectionHeader" x:Uid="SystemPage.PrivacySectionHeader" Style="{StaticResource SectionHeaderStyle}"/>
            <CheckBox x:Name="DisableClipboardCheckBox" x:Uid="DisableClipboardCheckBox.Content" Style="{StaticResource TweakerCheckBoxStyle}">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="DisableClipboardCheckBox.ToolTip"/>
                </ToolTipService.ToolTip>
            </CheckBox>

            <!-- Производительность -->
            <TextBlock x:Name="PerformanceSectionHeader" x:Uid="SystemPage.PerformanceSectionHeader" Style="{StaticResource SectionHeaderStyle}"/>
            <Button x:Name="SpeedUpWindowsButton" x:Uid="SpeedUpWindowsButton.Content" Style="{StaticResource TweakerButtonStyle}">
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="SpeedUpWindowsButton.ToolTip"/>
                </ToolTipService.ToolTip>
            </Button>

            <!-- Кнопка применения -->
            <Button x:Name="ApplyButton" x:Uid="ApplyButton.Content" Click="ApplyButton_Click" Style="{StaticResource TweakerButtonStyle}" Margin="0,10,0,0"/>

            <!-- Прогресс и статус -->
            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="5">
                <TextBlock x:Name="StatusText" Text="Выполняется..."/>
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0" 
                             Foreground="{ThemeResource SystemAccentColor}" Background="{ThemeResource SubtleFillColorSecondary}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>