<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PWin11_Tweaker_s.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <!-- Основной контейнер -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="600" Margin="20">
            <!-- Заголовок -->
            <TextBlock x:Uid="Welcome" 
                       FontSize="36" 
                       FontWeight="Bold" 
                       TextWrapping="Wrap" 
                       HorizontalAlignment="Center" 
                       Foreground="{ThemeResource SystemAccentColor}"/>

            <!-- Описание -->
            <TextBlock x:Uid="About"
                       FontSize="16" 
                       TextWrapping="Wrap" 
                       HorizontalAlignment="Center" 
                       Margin="0,20,0,30" 
                       Foreground="{ThemeResource TextFillColorSecondary}"/>

            <!-- Карточка с кнопкой -->
            <Border CornerRadius="10" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefault}" 
                    BorderThickness="1" 
                    Padding="20">
                <StackPanel>
                    <TextBlock x:Uid="Start_Use" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,10"/>
                    <Button x:Name="GoToPrivacyButton" 
                            x:Uid="GoToPrivacyButton"
                            Click="GoToPrivacyButton_Click" 
                            HorizontalAlignment="Stretch" 
                            Style="{StaticResource AccentButtonStyle}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>