<Page
    x:Class="PWin11_Tweaker_s.PerformancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="20" Spacing="10">
        <!-- Заголовок -->
        <TextBlock x:Name="TitleTextBlock" x:Uid="PerformancePage.Title" FontSize="24" FontWeight="SemiBold"/>

        <!-- Отключение визуальных эффектов -->
        <CheckBox x:Name="DisableVisualEffectsToggle" x:Uid="DisableVisualEffectsToggle.Content"/>

        <!-- Отключение Windows Search -->
        <CheckBox x:Name="DisableWindowsSearchToggle" x:Uid="DisableWindowsSearchToggle.Content"/>

        <!-- Отключение SysMain -->
        <CheckBox x:Name="DisableSysMainToggle" x:Uid="DisableSysMainToggle.Content"/>

        <!-- План электропитания -->
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock x:Name="PowerPlanLabel" x:Uid="PowerPlanLabel.Text" VerticalAlignment="Center"/>
            <ComboBox x:Name="PowerPlanCombo" SelectionChanged="PowerPlanCombo_SelectionChanged">
                <ComboBoxItem x:Uid="PowerPlanCombo.HighPerformance" Tag="HighPerformance"/>
                <ComboBoxItem x:Uid="PowerPlanCombo.Balanced" Tag="Balanced"/>
                <ComboBoxItem x:Uid="PowerPlanCombo.PowerSaver" Tag="PowerSaver"/>
            </ComboBox>
        </StackPanel>

        <!-- Кнопка применения -->
        <Button x:Name="ApplyButton" x:Uid="ApplyButton.Content" Click="ApplyButton_Click" Margin="0,10,0,0"/>

        <!-- Прогресс и статус -->
        <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="5">
            <TextBlock x:Name="StatusText" Text="Выполняется..."/>
            <ProgressBar x:Name="ProgressBar" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0"/>
        </StackPanel>
    </StackPanel>
</Page>