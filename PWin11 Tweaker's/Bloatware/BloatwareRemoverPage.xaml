<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PWin11_Tweaker_s.Bloatware.BloatwareRemoverPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s.Bloatware"
    xmlns:script="using:PWin11_Tweaker_s.Script"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:SizeConverter x:Key="SizeConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock 
            Grid.Row="0"
            Text="Удаление Bloatware"
            FontSize="24"
            FontWeight="Bold"
            Margin="0,0,0,20"/>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Фильтры и кнопки -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox x:Name="FilterComboBox" SelectedIndex="0" Margin="0,0,10,0" Width="150">
                    <ComboBoxItem Content="Все приложения"/>
                    <ComboBoxItem Content="UWP приложения"/>
                    <ComboBoxItem Content="OEM приложения"/>
                </ComboBox>
                <Button x:Name="RefreshButton" Content="Обновить" Click="RefreshButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="SelectAllButton" Content="Выбрать все" Click="SelectAllButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="DeselectAllButton" Content="Снять выбор" Click="DeselectAllButton_Click"/>
            </StackPanel>

            <!-- Список приложений -->
            <ListView 
                x:Name="BloatwareList"
                Grid.Row="1"
                SelectionMode="None"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                BorderThickness="1"
                BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                MaxHeight="500">
                <ListView.Header>
                    <Grid Background="{ThemeResource SystemControlBackgroundAltHighBrush}" Padding="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Выбрать" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Название" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Тип" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Размер" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Рекомендация" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="script:BloatwareItem">
                        <Grid Padding="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" Text="{x:Bind Type, Mode=OneWay}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{x:Bind Size, Mode=OneWay, Converter={StaticResource SizeConverter}}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="{x:Bind Recommendation, Mode=OneWay}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Кнопки управления и прогресс -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,10">
            <Button x:Name="RemoveSelectedButton" Content="Удалить выбранное" Click="RemoveSelectedButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="RestoreButton" Content="Восстановить UWP" Click="RestoreButton_Click" Margin="0,0,10,0"/>
            <ProgressBar x:Name="ProgressBar" Width="200" IsIndeterminate="False" Visibility="Collapsed" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Статус операций -->
        <TextBlock x:Name="StatusText" Grid.Row="3" Text="" FontSize="14" Foreground="Green" Margin="0,0,0,10"/>
    </Grid>
</Page>