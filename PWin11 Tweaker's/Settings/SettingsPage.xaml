<Page
    x:Class="PWin11_Tweaker_s.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Margin="20" Spacing="15">
        <!-- Заголовок -->
        <TextBlock Text="Настройки и Дебаг-лог" FontSize="24" FontWeight="Bold" />

        <!-- Консоль логов -->
        <Border BorderBrush="Gray" BorderThickness="1" Background="#1E1E1E" Padding="5" MaxHeight="400">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="LogItemsControl" ItemsSource="{x:Bind DebugMessages, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" Foreground="White" Margin="5,2" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Кнопка для проверки логирования -->
        <Button x:Name="TestLogButton" Content="Тест логирования" Click="TestLogButton_Click" Margin="0,10,0,0"/>

        <!-- Статус -->
        <TextBlock x:Name="StatusText" Text="" FontSize="14" Margin="0,10,0,0" Visibility="Collapsed"/>
    </StackPanel>
</Page>