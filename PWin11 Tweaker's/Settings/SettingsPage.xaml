<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PWin11_Tweaker_s.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PWin11_Tweaker_s"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="20" Spacing="15">
        <!-- Заголовок -->
        <TextBlock x:Name="TitleTextBlock" Text="Test Title (Should Be Visible)" FontSize="24" Foreground="Red" />

        <!-- Выбор языка -->
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock x:Name="LanguageLabel" Text="Test Label: (Should Be Visible)" Foreground="Red" />
            <ComboBox x:Name="LanguageComboBox" SelectionChanged="LanguageComboBox_SelectionChanged">
                <ComboBoxItem x:Name="RussianItem" Tag="ru-RU" Content="Test Russian (Should Be Visible)" />
                <ComboBoxItem x:Name="EnglishItem" Tag="en-US" Content="Test English (Should Be Visible)" />
            </ComboBox>
        </StackPanel>

        <!-- Лог сообщений -->
        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,10,0,0" MaxHeight="150">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{x:Bind DebugMessages, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" Margin="5,2" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </StackPanel>
</Page>